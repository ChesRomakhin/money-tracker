import * as React from "react";
import {ApplicationContext} from "../ApplicationContext/ApplicationContext";

export const GreenBeanReactContext: React.Context<ApplicationContext> = React.createContext(null as any);